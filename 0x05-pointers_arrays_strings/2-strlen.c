#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 * @s: string provided
 * Return: Always 0.
 */
int _strlen(char *s)
{
	int i = 0, len;

	for (c = s[i] ; *s != '\0'; i++)
	{
		len = i + 1;
	}
	return (len);
}
