#include <stdio.h>
#include "main.h"

/**
 * _islower - checks for lowercase character
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
char c;
if (c > 32)
{
printf("%d", 1);
}
else
{
printf("%d", 0);
}
}
