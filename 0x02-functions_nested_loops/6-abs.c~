#include <stdio.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * Return: always 0
 */

int _abs(int);
int _abs(int n)
{
if (n >=0)
{
return (n);
}
else
{
return (-n);
}
}
