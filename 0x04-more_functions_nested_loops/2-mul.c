#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two integers.
 * @a: first int to multiple
 * @b: second int to multiple
 * Return: the result 
 */
int mul(int a, int b)
{
return (a*b);
}
