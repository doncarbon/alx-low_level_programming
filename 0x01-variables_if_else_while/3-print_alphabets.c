#include <stdio.h>

/**
 * main - prints the alphabets in lowercase
 * Return: Always 0 (Success)
 */

int main(void)
{
char alphabet;
alphabet = 'a';
ua = 'A';
while (alphabet <= 'z')
{
putchar(alphabet);
alphabet++;
}
while (ua <= 'Z')
{
putchar(ua);
ua++;
}
putchar("\n");
return (0);
}
